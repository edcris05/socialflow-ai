<x-layouts.app title="{{ $entry->title }} · SocialFlow AI">
    <div class="flex flex-col justify-between gap-4 sm:flex-row sm:items-start"><div><p class="text-sm font-semibold uppercase tracking-[0.2em] text-indigo-600">{{ $brand->name }}</p><h1 class="mt-2 text-3xl font-semibold">{{ $entry->title }}</h1><p class="mt-2 text-sm text-slate-500">{{ $entry->status }} · {{ $entry->source ?: 'Sin fuente' }}</p></div><a href="{{ route('marcas.conocimiento.edit', [$brand, $entry]) }}" class="rounded-lg border border-slate-300 bg-white px-4 py-2.5 text-sm font-semibold">Editar</a></div>
    <article class="mt-8 whitespace-pre-line rounded-xl border border-slate-200 bg-white p-6 leading-7 shadow-sm">{{ str_replace('\\n', "\n", $entry->content) }}</article>
    @if ($entry->status !== 'verified')<form action="{{ route('marcas.conocimiento.verify', [$brand, $entry]) }}" method="POST" class="mt-4">@csrf @method('PATCH')<button class="rounded-lg bg-emerald-700 px-4 py-2.5 text-sm font-semibold text-white">Marcar como verificado</button></form>@endif
    <section class="mt-8"><h2 class="text-lg font-semibold">Historial</h2><div class="mt-3 space-y-2">@foreach ($entry->audits()->latest()->get() as $audit)<div class="rounded-lg border border-slate-200 bg-white p-4 text-sm"><strong>{{ $audit->action }}</strong> por {{ $audit->user->name }} · {{ $audit->created_at }}</div>@endforeach</div></section>
</x-layouts.app>