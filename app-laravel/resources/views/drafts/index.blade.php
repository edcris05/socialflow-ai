<x-layouts.app title="Borradores de {{ $brand->name }} · SocialFlow AI">
    <div class="flex items-center justify-between gap-4"><div><p class="text-sm font-semibold uppercase tracking-[0.2em] text-indigo-600">Borradores</p><h1 class="mt-2 text-3xl font-semibold">{{ $brand->name }}</h1></div><a href="{{ route('marcas.borradores.create', $brand) }}" class="rounded-lg bg-slate-950 px-4 py-2.5 text-sm font-semibold text-white">Nuevo borrador</a></div>
    <div class="mt-8 space-y-3">@forelse ($drafts as $draft)<a href="{{ route('marcas.borradores.edit', [$brand, $draft]) }}" class="block rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div class="flex justify-between gap-4"><h2 class="font-semibold">{{ $draft->title }}</h2><span class="text-sm text-slate-500">{{ $draft->status }}</span></div><p class="mt-2 line-clamp-2 text-sm text-slate-600">{{ $draft->content }}</p></a>@empty<div class="rounded-xl border border-dashed border-slate-300 bg-white px-6 py-12 text-center text-slate-600">No hay borradores.</div>@endforelse</div>
</x-layouts.app>